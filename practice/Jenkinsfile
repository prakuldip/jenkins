pipeline {
    agent any 
    stages {
        stage('printin headline') {
            steps {
                echo "Dear all, my name is `whoami` and my hostname is `hostname`" 
                sh './printdate.sh'
            }
        }
    }
}
