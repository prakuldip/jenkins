pipeline {
    agent any 
    stages {
        stage('printin headline') {
            steps {
                sh echo "Dear all, my name is $(whoami) and my hostname is $(hostname)" 
                sh echo "I am currently in $(pwd) location"
            }
        }
    }
}
